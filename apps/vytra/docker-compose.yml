services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: vytra
      POSTGRES_PASSWORD: Vytra!2026
      POSTGRES_DB: vytra
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
